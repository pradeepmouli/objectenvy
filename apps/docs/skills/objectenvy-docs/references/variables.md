# Variables & Constants

## Parsing

### `defaultNonNestingPrefixes`
Prefix segments that are never treated as nesting roots regardless of how many env vars share them.
```ts
const defaultNonNestingPrefixes: string[]
```
