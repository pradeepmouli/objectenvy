{
  "name": "objectenvy-workspace",
  "version": "1.0.0",
  "description": "ObjectEnvy monorepo - strongly-typed environment configuration for TypeScript projects",
  "keywords": [
    "config",
    "configuration",
    "env",
    "environment",
    "typescript",
    "type-safe",
    "zod",
    "nested",
    "camelcase",
    "dotenv"
  ],
  "private": true,
  "license": "MIT",
  "homepage": "https://pradeepmouli.github.io/objectenvy/",
  "bugs": {
    "url": "https://github.com/pradeepmouli/objectenvy/issues"
  },
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "pnpm -r lint",
    "format": "pnpm -r format",
    "format:check": "pnpm -r format:check",
    "type-check": "pnpm -r type-check",
    "clean": "pnpm -r clean && rm -rf dist coverage",
    "docs:dev": "pnpm --filter @objectenvy/docs dev",
    "docs:build": "pnpm --filter @objectenvy/docs build",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "pnpm build && pnpm -r --filter=objectenvy --filter=objectenvy-cli publish",
    "changeset:version": "changeset version",
    "changeset:publish": "pnpm run build && changeset publish"
  },
  "devDependencies": {
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@to-skills/cli": "^0.3.13",
    "@to-skills/vitepress": "^0.2.25",
    "@typescript/native-preview": "beta",
    "@vitest/coverage-v8": "^4.1.5",
    "lint-staged": "^17.0.4",
    "oxfmt": "^0.48.0",
    "oxlint": "^1.63.0",
    "tslib": "^2.8.1",
    "tsx": "^4.21.0",
    "type-fest": "^5.6.0",
    "typedoc": "^0.28.19",
    "typedoc-plugin-markdown": "^4.11.0",
    "typedoc-plugin-to-skills": "^1.3.0",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "zod": "^4.4.3"
  },
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=11.0.0"
  },
  "packageManager": "pnpm@11.1.3",
  "author": "Pradeep Mouli <pmouli@mac.com> (https://github.com/pradeepmouli)"
}
