{
  "name": "@objectenvy/env-y-config-legacy",
  "version": "1.0.0",
  "description": "CLI tool to generate .env files from schemas (Zod, JSON Schema, TypeScript, JSON)",
  "keywords": [
    "env",
    "environment",
    "dotenv",
    "schema",
    "generator",
    "zod",
    "json-schema",
    "typescript",
    "cli"
  ],
  "license": "MIT",
  "author": "",
  "type": "module",
  "bin": {
    "env-y-config": "./bin/env-y-config.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist",
    "bin"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:native": "tsgo -p tsconfig.json",
    "dev": "tsx watch src/cli.ts",
    "check:native": "tsgo --noEmit -p tsconfig.json",
    "test": "vitest",
    "test:coverage": "vitest --coverage",
    "lint": "oxlint src",
    "format": "oxfmt src",
    "format:check": "oxfmt --check src"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "ts-morph": "^21.0.1"
  },
  "devDependencies": {
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.9.0",
    "vitest": "^2.0.0"
  },
  "peerDependencies": {
    "zod": "^4.3.4"
  },
  "peerDependenciesMeta": {
    "zod": {
      "optional": true
    }
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
